"use strict"

window.BrowserRowSubtitle = TextField.extend().newSlots({
    type: "BrowserRowSubtitle",
}).setSlots({
    init: function () {
        TextField.init.apply(this)
        return this
    },

})


